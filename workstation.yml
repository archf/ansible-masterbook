---

  # one must sudo su to run as root on localhost from non-root that is why we
  # need sudo
- hosts: workstations
  remote_user: root
  become: yes
  # roles:
  #   - { role: accounts, accounts_profile: vl_account.yml }
      # - accounts
    # - { role: accounts, useraccountfiles: [ default.yml ]}

  vars_files:
    - private/users/accounts.yml

  roles:
    - accounts

- hosts: workstations
  remote_user: archf
  become: yes

  roles:
  - common
  - synergy
  - workstation
  - docker

  # - network
    # - dotfiles
  tasks:
    - include: task_lib/virtualbox_vl.yml
