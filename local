# stub inventory
# this is to run ansible on self
#
# groups are added for plays targeting groupes (i.e hosts != all)

localhost ansible_ssh_host=localhost ansible_connection=local

[gw]
gw00  ansible_ssh_host=192.168.3.1

[workstations]
localhost ansible_ssh_host=localhost ansible_connection=local

